#include <stdio.h>
int main(){
    int x = 30,y = 40;
    printf("The value of x is %d.",x);
    printf("\nThe value of y is %d.",y);
    if (x == y)
        printf("\nx is equal to y.\n");
    else if (x > y)
        printf("\nx is greater than y.\n");
    else  (x < y);
        printf("\nx is less than y.\n");

    return 0;
}
